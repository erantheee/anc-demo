# 后续功能

这些功能不进入当前 Raspberry Pi ANC Demo 的 M1/M2 版本。

## 实时 ANC 环路（M2）

- [ ] 接入 ALSA 低延迟音频循环：WM8960 I2S 编解码器，参考麦 ADC + 误差麦 ADC + DAC 输出。
- [ ] 实时 FXLMS：ALSA 小 period、`sched` 高优先级线程、禁用 PipeWire 重采样。
- [ ] 次级路径 S(z) 在线辨识（播放探测信号，误差麦回采）。
- [ ] 谐波消除器频率跟踪（FLL/PLL）：当前为离线静态 f0，实时需持续跟踪。
- [ ] 实时 A/B 评估：ANC 开关自动切换并出报告。

进入条件：M0 硬件就绪（I2S 编解码器到货、音频 I/O 验证通过）。

## 空间建模（M3）

- [ ] 摄像头标定（棋盘格）与地面平面检测。
- [ ] ArUco 打印机定位 → 房间坐标（误差 < 30 cm）。
- [ ] 打印状态检测（帧间运动估计）。
- [ ] 静音区在地图上点选/推荐，联动 ANC 参数与可行性提示。
- [ ] RPLIDAR 2D 房间地图接入 `RoomModel.lidar_map`。
- [ ] 麦克风阵列 DOA 辅助定位。

进入条件：M2 实时 ANC 可稳定运行。

## 迭代场景（M4）

- [ ] 空调：50/60 Hz 谐波消除专项测试。
- [ ] 硬件工程师风枪：宽带强噪声测量协议（含 SPL 阈值判断）。
- [ ] 风机：BPF 谐波消除专项测试。
- [ ] 门外扫地机器人：结构传导参考麦方案与移动源跟踪。

## 产品化

- [ ] 测量报告自动邮件/Web 推送。
- [ ] 单次测量超时恢复、日志轮转。
- [ ] 降噪前后听感盲评模板。

## 已知边界

- 静态谐波消除对基频漂移敏感（离线估计精度 0.1 Hz 量级），实时需要频率跟踪。
- 来源归属在频谱高度重叠的源之间（3D 打印机 vs 风机）只给出概率排序，需人工确认。
- 未标定 SPL 时为 dBFS（相对值），标定后才能显示绝对 dB SPL。

## 噪声检测 Agent 后续

- [ ] **LLM 质量 eval 基线**：为 `agent.py` 的 SYSTEM_PROMPT + AGENT_TOOLS 建立离线质量 eval——
  一组固定合成噪声场景（打印机/风扇/啸叫/静音），真实 API Key 跑 Agent，断言 JSON schema
  完整、signal_class 正确、啸叫场景 anc_worthwhile=false。提示词/工具 schema 是 LLM 面向的，
  改了没有质量回归保护；现有 stub 测试只覆盖机械行为（工具循环、JSON 解析），不验证输出质量。
  依赖：无，建议在代码上传 GitHub 后启动。
- [ ] **P1 Agent 自主周期判断**：Agent 按固定周期（如每 N 分钟）自动跑一次检测，发现啸叫/
  降噪失效时自主调增益，异常时通知。用户明确担忧决策错误，推迟到上传 GitHub + 真机 demo
  验证残差分析有效之后。闭环能力（adjust_anc + watchdog 边界 + 残差分析）本批已就位，
  启动时只需加调度器 + 决策确认机制。
